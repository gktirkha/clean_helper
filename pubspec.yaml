name: clean_helpers
description: A CLI tool for scaffolding Flutter clean architecture projects.
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.11.3

executables:
  init: clean_helpers
  create-repo: create_repo

dependencies:
  path: ^1.9.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
